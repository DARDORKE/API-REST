Dans votre application, créez les endpoints d'API REST permettant de traiter les requêtes GET, POST, PUT et
DELETE sur une ressource User et de renvoyer les réponses adaptées.

L'entité User devra être créée au préalable et contenir les propriétés « id », « firstName », « lastName »,
« age » et « createdAt » (date de création).

Testez ensuite tous vos endpoints avec Postman.